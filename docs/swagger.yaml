basePath: /api/v1
host: localhost:8080
info:
  contact: {}
  description: Hệ thống quản lý REST API + WebSocket backend phục vụ quản lý camera
    và nhận diện.
  title: Fall Detection API
  version: "1.0"
paths: {}
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
